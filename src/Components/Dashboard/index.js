import React from "react";
import FileDrawer from '../Drawer';

function Dashboard() {
    return (
        <div>
            <FileDrawer></FileDrawer>
        </div>
    );
}

export default Dashboard;
